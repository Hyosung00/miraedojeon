{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend API for North Korea attack data",
  "main": "mongoDB_server.js",
  "scripts": {
    "start": "concurrently \"node mongoDB_server.js\" \"py -m uvicorn Neo4j_server:app --reload --port 8000\"",
    "dev": "nodemon mongoDB_server.js",
    "pyserver": "py -m uvicorn Neo4j_server:app --reload --port 8000",
    "build": "echo 'No build step required for backend JS. Use npm start.'"
  },
  "dependencies": {
    "concurrently": "^9.2.1",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "neo4j-driver": "^5.28.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2",
    "concurrently": "^8.2.2"
  }
}
